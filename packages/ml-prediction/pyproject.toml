[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "drishti-predict"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["xgboost>=2.0", "numpy>=1.26", "scikit-learn>=1.4"]

[tool.hatch.build.targets.wheel]
packages = ["src/drishti_predict"]
